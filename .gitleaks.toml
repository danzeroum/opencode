title = "opencode gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Fake secrets in skill-vetter test fixtures, and generated SDK client code (regenerated from the public openapi.json contract — no real secrets; matches are generated identifiers with incidental entropy)"
paths = [
  '''script/skills/fixtures/.*''',
  '''packages/sdk/js/src/gen/.*''',
  '''packages/sdk/js/src/v2/gen/.*''',
]
